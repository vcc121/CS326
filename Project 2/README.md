JSON file saving is linked to my personal directory, so the user should change the directory according to their own directory.

Runtime does NOT print to the terminal. To find details like this, they will be printed into the JSON file
